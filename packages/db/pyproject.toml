[project]
name = "db"
version = "0.1.0"
description = "Shared database models and utilities package"
requires-python = ">=3.10"

dependencies = [
    "sqlalchemy>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
